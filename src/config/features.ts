/**
 * Feature flags — modifiables par l'équipe marketing sans toucher au code.
 * showBoostButton: affiche le bouton "Booster" sur les produits, services, annonces.
 */
export const FEATURES = {
  showBoostButton: true,
} as const;
